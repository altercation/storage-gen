drive
    partition --size 100M
        filesystem
    partition --label "My     long    label"
        filesystem
    partition --noclobber --devpath /dev/sdb1
        filesystem
drive
    partition --size 100M
        filesystem
    partition --label "My     long    label"
        filesystem
    partition --noclobber --devpath /dev/sdb1
        filesystem
