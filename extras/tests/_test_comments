# a comment
    # a comment
drive --label "a label for a drive" --bootable
    # a comment
    partition --label "test" --size 100M --noclobber
    partition --size 200M --label "MyLabel  Has    Weird Spaces" # some comment here
        encryption --label=cryptsystem
            filesystem --fstype=btrfs --label=system
                subvolume --label=svroot --mountpoint=/
                subvolume --label=svhome --mountpoint=/home
    partition --label "My Label \
 is split"
        encryption --label=\
AnotherLine
drive --label MyDrive --devpath /dev/sdx
    partition --label "MyDrive \
testing split"
        filesystem
drive
    partition --mountpoint /

